from apps.api.core.database import Base
from apps.api.db.models import (
    UserModel, ProfileModel, GroupModel, GroupMemberModel,
    TripModel, LocationPointModel, AlertModel, RecommendationModel, SOSAlertModel
)
