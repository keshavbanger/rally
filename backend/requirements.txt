# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# --- Database (Supabase Postgres) ---
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
geoalchemy2>=0.14.0
alembic>=1.13.0
# Pure-Python route geometry (nearest-point-on-line, projection) for live
# route matching — deliberately not a PostGIS round trip per GPS update,
# see app/route/matcher.py's module docstring.
shapely>=2.0.0

# --- Config & validation ---
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Auth (verifying Supabase-issued JWTs only) ---
pyjwt[crypto]>=2.8.0

# --- Redis (live trip state, presence, pub/sub) ---
redis>=5.0.0

# --- WebSockets (uvicorn[standard] above already bundles a driver, but
# pin the websockets package explicitly since the app depends on it directly) ---
websockets>=12.0

# --- Weather integration (app/weather/service.py) — outbound HTTP to the
# configured weather provider only; also used by the test suite's
# TestClient, so this was already an installed dependency before Phase 12. ---
httpx>=0.27.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
fakeredis>=2.20.0

# Not yet: numpy, pandas, scikit-learn, osrm client — added when their
# respective phases start.
