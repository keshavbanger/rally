import TripAnalyticsInteractive from '@/components/marketing/TripAnalyticsInteractive';

export const metadata = {
  title: 'Trip Analytics — RALLY',
  description: 'Every trip leaves a record — where you went, how it went, and what happened along the way.',
};

export default function TripAnalyticsDirectPage() {
  return <TripAnalyticsInteractive />;
}
