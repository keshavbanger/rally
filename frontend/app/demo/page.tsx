import DemoInteractive from '@/components/demo/DemoInteractive';

export const metadata = {
  title: 'Demo — RALLY',
  description: 'See what RALLY looks like once your group is live — no login required.',
};

export default function DemoPage() {
  return <DemoInteractive />;
}
