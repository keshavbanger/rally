import LiveTrackingRedesign from '@/components/marketing/LiveTrackingRedesign';

export const metadata = {
  title: 'Live Tracking — RALLY LIVE',
  description: "One shared map for the whole trip — see every member's position, planned route, and destination in real time.",
};

export default function LiveTrackingPage() {
  return <LiveTrackingRedesign />;
}
