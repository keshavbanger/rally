import GroupHealthInteractive from '@/components/marketing/GroupHealthInteractive';

export const metadata = {
  title: 'Group Health — RALLY',
  description: 'One clear read on how your group is doing, right now.',
};

export default function GroupHealthDirectPage() {
  return <GroupHealthInteractive />;
}
