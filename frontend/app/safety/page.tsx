import SafetyInteractive from '@/components/safety/SafetyInteractive';

export const metadata = {
  title: 'Safety — RALLY',
  description: 'How RALLY treats location data, group membership, and the one-tap SOS.',
};

export default function SafetyPage() {
  return <SafetyInteractive />;
}
