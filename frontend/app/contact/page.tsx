import ContactInteractive from '@/components/contact/ContactInteractive';

export const metadata = {
  title: 'Contact — RALLY',
  description: "Questions, feedback, or something not working right — we'd like to hear about it.",
};

export default function ContactPage() {
  return <ContactInteractive />;
}
